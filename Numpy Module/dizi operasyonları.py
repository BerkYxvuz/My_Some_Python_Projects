import numpy as np
'''
numbers1 = np.random.randint(10, 100, 6)
numbers2 = np.random.randint(10, 100, 6)

# print(f'{numbers1} - {numbers2}')

result = numbers1 + numbers2 #bütün indeksleri toplar tek bir değer oluşur.
result = numbers1 + 10 #bütün indekslere 10 ekler
result = numbers1 - numbers2 #bütün indeksleri birbirinden çıkar
result = numbers1 - 10 #bütün indekslerden 10 çıkarır
result = numbers1 * 2 #hepsini 2 ile çarpar
result = numbers1 / 2 #hepsini 2 ile böler

result = np.sin(numbers1) #sinüsünü alır
result = np.cos(numbers1) #kosünüsü alır
result = np.sqrt(numbers1) #karekökünü alır
result = np.log(numbers1) # her bir değerin karşılığını hesaplar?
mnumbers1 = numbers1.reshape(2, 3)
mnumbers2 = numbers2.reshape(2, 3)

result = np.vstack((mnumbers1, mnumbers2)) # dikey olarak birleştirme
result = np.hstack((mnumbers1, mnumbers2)) # yatay olarak birleştirme

result = numbers1 >= 50 #her indexi 50 ten büyük mü diye kontrol eder
result = numbers1 % 2 == 0 # her indexi çift mi diye kontrol eder
'''